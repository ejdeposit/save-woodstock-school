<nav>
  <div class="nav-bar">
    <button id="mobile-menu">
      <i class="fa fa-bars fa-2x"></i>
    </button>
    <div id="links">
      {{#navBar.links}}
      <div><a href="{{{path}}}">{{name}}</a></div>
      {{/navBar.links}}
    </div>
    <div><a id="translated_site" href="{{{navBar.translatedSitePath}}}">{{navBar.translatedSite}}</a></div>
  </div>
  <div class="hidden drop-down">
    {{#navBar.links}}
    <div><a href="{{{path}}}">{{name}}</a></div>
    {{/navBar.links}}
  </div> 
</nav>